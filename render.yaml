services:
  - type: web
    name: custom-vision-tagger
    runtime: python
    pythonVersion: "3.10.14"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.14"
